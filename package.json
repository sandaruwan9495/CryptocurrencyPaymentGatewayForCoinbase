{
  "name": "coinbase_payment_gateway",
  "version": "1.0.0",
  "description": "This is an API for handling the cryptocurrency transaction in the existing projects.Coinbase is the wallet and in here we use their API for do the stuff.",
  "main": "index.js",
  "scripts": {
    "start":"node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "set NODE_ENV=development&&set NODE_PORT=3000&&nodemon start",
    "prod": ""
  },
  "author": "G.D.S.Wijerathne",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.1.0",
    "express": "^4.16.4"
  }
}
